{
  "name": "fact-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "build:web": "npm --workspace apps/web run build",
    "start:web": "npm --workspace apps/web run start",
    "deploy": "npm run build:web && git add apps/ package.json .github/ DEPLOY.md && (git diff --cached --quiet || git commit -m \"chore: deploy\") && git push origin main && cd apps/web && vercel deploy --prod --yes"
  }
}
